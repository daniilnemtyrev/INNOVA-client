import React from 'react';
import { Main } from '../../styles/general';
import { LoginFormik } from './ui/login-formik';

export const LoginMain = () => {
  return (
    <Main>
      <LoginFormik />
    </Main>
  );
};
